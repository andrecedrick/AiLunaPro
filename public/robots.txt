# AiLunaPro robots policy.
# Public, indexable surfaces (e.g. /audit-express) are crawlable.
# The authenticated SPA app shell (served from /) is excluded from indexing
# via a `noindex` meta + X-Robots-Tag header, not via Disallow (the app is
# hash-routed, so path-based Disallow cannot target app views).
User-agent: *
Allow: /
